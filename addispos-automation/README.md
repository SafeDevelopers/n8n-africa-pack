# addispos-automation

Scaffold for Addispos automation project. This directory contains:

- `workflows/` — placeholder JSON workflow files (13 files)
- `frontend/` — tiny Vue PWA scaffold

You can add sample workflow JSON to the `workflows/` files and implement frontend code in `frontend/src`.
